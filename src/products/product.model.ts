export class productModel{

    //typescript automatically adds the properties to the class.
    constructor(public id: string, public title: string,
        public description: string, public price: number){

        }
}